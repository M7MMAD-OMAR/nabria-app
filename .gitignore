__pycache__/
*.pyc

# Built by scripts/release-engine.sh and uploaded to a release; the checksum
# in engine/CHECKSUMS is what belongs in git, not 57 MB of binary.
whisper-server-linux-*
dist/

# wrangler deploy scratch; nothing here is worth a commit.
.wrangler/

# Native Windows runtime staging and engine compilation.
build/
